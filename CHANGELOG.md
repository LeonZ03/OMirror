# 更新日志

## 1.15.0 — 2026-09-07

- 设备来源由固定配置改为动态 USB ADB 发现，首次连接新手机时可直接保存并启动投屏。
- 增加已连接、未连接、新设备、等待授权和设备离线状态。
- 设备列表支持离线历史记录、四行以上滚动和本地删除；删除不会修改手机数据。
- 当前设备改为按 ADB 序列号保存，设备库使用 `%LOCALAPPDATA%\OMirror\devices.json` 原子写入。
- 排除无线调试和模拟器，并兼容 Windows ADB 不显示 `usb:` 标记的有线设备。
- 删除设备时同步停止对应投屏并作废旧的恢复任务，避免延迟回调重新启动已删除设备。
- 自检现在覆盖 ADB、scrcpy、USB 枚举解析和设备库读写；生命周期随机测试加入设备删除与重新添加场景。

升级说明：1.15.0 不导入旧版 `devices.local.txt`，升级后需要在设备列表中重新添加手机。其他通用偏好不受影响。
